# Claude Code Reference - Python Dependencies
#
# GTK3 bindings are system packages, not pip packages.
# Install with: sudo apt install python3-gi python3-gi-cairo gir1.2-gtk-3.0
#
# This file is for documentation purposes.
# The install.sh script handles dependency installation automatically.

# System packages required (install via apt):
# - python3-gi
# - python3-gi-cairo
# - gir1.2-gtk-3.0

# No pip packages required - uses only standard library and system GTK bindings
